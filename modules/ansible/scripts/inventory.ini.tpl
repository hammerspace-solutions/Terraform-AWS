# Ansible Inventory for SSM Connection - Generated by Terraform
# This file is dynamically generated. Do not edit manually.

[all:vars]
# Tell Ansible to use the AWS SSM connection plugin for all hosts.
ansible_connection=aws_ssm

# The default user for SSM sessions is 'root', which has the necessary permissions.
ansible_user=root

[clients]
%{ for instance in clients ~}
${instance.id}
%{ endfor ~}

[storage_servers]
%{ for instance in storage_servers ~}
${instance.id}
%{ endfor ~}

[ecgroup_nodes]
%{ for instance in ecgroup_nodes ~}
${instance.id}
%{ endfor ~}

